<%= form_for @schoolhouse do |f| %>

	<%= f.label :name %>
	<%= f.text_field :name, autofocus: true %>

	<%= f.label :level %>
	<%= f.text_field :level %>

	<%= f.label :enrollment %>
	<%= f.text_field :enrollment %>

	<%= f.submit class: "waves-effect waves-light btn" %>

<% end %>